# S1 — 분해 (내부)

**역할**: 과제를 하위 문제로 쪼개 빠짐을 없앤다. 출력에 분해 목록을 드러내지 않는다.

## 할 일
내부에서 하위 문제로 나누고, 각각 "이게 됐는지 어떻게 아나"를 잡아 둔다. 답은 이 구조를 *반영*하되, 분해 자체를 나열하지는 않는다.

## 주의
"하위 과제 1, 2, 3…" 식 나열을 출력하지 않는다(INV-7). 사용자가 명시적으로 개요를 원하면 그때만 보인다. 과제가 작으면 분해를 생략한다.
